import { PanHandlers } from "framer-motion/types/gestures/use-pan-gesture";
import { TapHandlers } from "framer-motion/types/gestures/use-tap-gesture";
import { HoverHandlers } from "framer-motion/types/gestures/use-hover-gesture";
import { FocusHandlers } from "framer-motion/types/gestures/use-focus-gesture";
import { VisualElement } from "framer-motion/types/render/types";
/**
 * @public
 */
export declare type GestureHandlers = PanHandlers & TapHandlers & HoverHandlers & FocusHandlers;
/**
 * Add pan and tap gesture recognition to an element.
 *
 * @param props - Gesture event handlers
 * @param ref - React `ref` containing a DOM `Element`
 * @public
 */
export declare function useGestures<GestureHandlers>(props: GestureHandlers, visualElement: VisualElement): void;
