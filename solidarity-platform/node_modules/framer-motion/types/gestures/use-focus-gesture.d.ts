import { TargetAndTransition } from "framer-motion/types/types";
import { MotionProps, VariantLabels } from "framer-motion/types/motion/types";
import { VisualElement } from "framer-motion/types/render/types";
/**
 * @public
 */
export interface FocusHandlers {
    /**
     * Properties or variant label to animate to while the focus gesture is recognised.
     *
     * @motion
     *
     * ```jsx
     * <motion.input whileFocus={{ scale: 1.2 }} />
     * ```
     */
    whileFocus?: VariantLabels | TargetAndTransition;
}
/**
 *
 * @param props
 * @param ref
 * @internal
 */
export declare function useFocusGesture({ whileFocus }: MotionProps, visualElement: VisualElement): void;
