import * as React from "react";
import { MotionProps } from "framer-motion/types/motion/types";
import { RenderComponent, MotionFeature } from "framer-motion/types/motion/features/types";
import { CreateVisualElement } from "framer-motion/types/render/types";
export { MotionProps };
export interface MotionComponentConfig<E> {
    defaultFeatures: MotionFeature[];
    createVisualElement: CreateVisualElement<E>;
    useRender: RenderComponent;
}
/**
 * Create a `motion` component.
 *
 * This function accepts a Component argument, which can be either a string (ie "div"
 * for `motion.div`), or an actual React component.
 *
 * Alongside this is a config option which provides a way of rendering the provided
 * component "offline", or outside the React render cycle.
 *
 * @internal
 */
export declare function createMotionComponent<P extends {}, E>({ defaultFeatures, createVisualElement, useRender, }: MotionComponentConfig<E>): React.ForwardRefExoticComponent<React.PropsWithoutRef<P & MotionProps> & React.RefAttributes<E>>;
