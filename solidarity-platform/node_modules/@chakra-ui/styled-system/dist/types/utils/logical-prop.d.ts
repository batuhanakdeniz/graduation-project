import * as CSS from "csstype";
import { PropConfig } from "@chakra-ui/styled-system/dist/types/core";
declare type CSSProp = keyof CSS.Properties;
interface LogicalOptions<T> {
    scale?: string;
    property: {
        ltr: T | T[];
        rtl: T | T[];
    };
    transform?: PropConfig["transform"];
}
export declare function logical<T extends CSSProp>(opts: LogicalOptions<T>): PropConfig;
export {};
