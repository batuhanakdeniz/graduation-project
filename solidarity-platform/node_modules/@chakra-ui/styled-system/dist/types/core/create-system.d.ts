import { Config, Parser, PropConfig } from "@chakra-ui/styled-system/dist/types/core/types";
export declare function system<T extends PropConfig | Config>(configs: T): Parser;
export declare function compose(...parsers: Parser[]): Parser;
