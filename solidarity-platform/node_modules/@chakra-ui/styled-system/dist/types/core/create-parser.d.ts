import { Dict } from "@chakra-ui/utils";
import { PropConfig } from "@chakra-ui/styled-system/dist/types/core/types";
export declare function createParser(config: PropConfig): {
    (props: Dict): Record<string, any>;
    config: PropConfig;
    propNames: string[];
    cache: Map<string, any>;
};
