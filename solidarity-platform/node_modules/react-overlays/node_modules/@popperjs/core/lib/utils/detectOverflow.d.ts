import { State, SideObject, Padding } from "react-overlays/node_modules/@popperjs/core/lib/types";
import { Placement, Boundary, RootBoundary, Context } from "react-overlays/node_modules/@popperjs/core/lib/enums";
export declare type Options = {
    placement: Placement;
    boundary: Boundary;
    rootBoundary: RootBoundary;
    elementContext: Context;
    altBoundary: boolean;
    padding: Padding;
};
export default function detectOverflow(state: State, options?: Partial<Options>): SideObject;
